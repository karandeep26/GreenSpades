<?xml version="1.0" encoding="utf-8"?>

<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- This is the line -->
    <item android:top="@dimen/negative_dimen" android:right="@dimen/negative_dimen"
          android:left="@dimen/negative_dimen">
        <shape>
            <solid android:color="@android:color/transparent" />
            <stroke android:width="@dimen/bottom_dimen" android:color="@color/second_color" />
        </shape>
    </item>

</layer-list>